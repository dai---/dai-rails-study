<%= form_for(@co) do |f| %>
  <% if @co.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@co.errors.count, "error") %> prohibited this co from being saved:</h2>

      <ul>
      <% @co.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <%= f.number_field :status %>
  </div>
  <div class="field">
    <%= f.label :item_group_id %><br>
    <%= f.text_field :item_group_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
